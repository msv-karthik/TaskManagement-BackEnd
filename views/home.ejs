<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <nav class="navbar bg-dark border-bottom border-body" data-bs-theme="dark">
        <div class="container-fluid">
            <div class="d-flex ms-auto">
                <button class="btn btn btn-info" type="button" onclick="window.location.href='/addTask'" >Add Task</button>
            </div>
        </div>
    </nav>

    <h1 class="main-heading">
        <% if (tasks.length === 0) { %>
            No tasks added.
        <% } %>
    </h1>
    

    <div class="card-container">
        <% tasks.forEach(task => { %>
            <div class="card <%= task.completed ? 'completed' : '' %>" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title"><%= task.category %></h5>
                    <p class="card-text"><%= task.task %></p>
                    <button class="btn btn-secondary" onclick="window.location.href='/markCompleted/<%= task._id %>'">Completed</button>
                    <button class="btn btn-primary" onclick="window.location.href='/editTask/<%= task._id %>'" 
                        <%= task.completed ? 'disabled' : '' %> 
                        style="<%= task.completed ? 'cursor: not-allowed;' : '' %>">Edit</button>
                    <button class="btn btn-danger" onclick="window.location.href='/deleteTask/<%= task._id %>'">Delete</button>
                </div>
            </div>
        <% }) %>
    </div>
    
</body>
</html>
